# v0.0.2 GA gap list — 2026-09-19

Assessed at master `3a1982e` (PR #253, DJI-501). Every Beta Readiness issue in
Linear is Done; the acceptance record is current through the 2026-09-18 runs
plus the DJI-501 resolution. This list is what a GA cut would still want.

## A. Product / behavior gaps

1. ~~**The fallback entrance's identity-gate refusal is test-proven, not
   run-proven.**~~ **ADDRESSED (PR #257, `1603632`):** the e2e stack now serves
   a real but mismatched-tagged FLAC from a documentation-IPv6 sidecar, the
   download passes the guard AND the boundary, and the identity gate refuses
   it live — with a mutation check proving the spec binds to the gate. The
   acceptance record's fallback row carries the evidence.
2. **The allowlist is a beta default, not a GA feature.**
   `ops/squid/allowed-domains.txt` ships three YouTube domains. A GA cut needs
   a documented per-deployment story (which domains, who edits, restart
   procedure — the restart half is documented now) and the multi-hop
   public→private post-handover refusal driven live (recorded honest limit).
3. **The concurrency limit is hardcoded.** The worker runs up to
   `MaxConcurrentJobs = 5` acquisitions at once — a Go constant in
   `cmd/worker/main.go`, not config. The acceptance record's finding 2 asks
   that it "be made explicit"; a GA cut should make it an env setting with a
   documented default (and keep the existing SQLite>1 warning).
4. **`getAlbum` songs report `duration="0"` and an empty `contentType`.** The
   one open client-visible Subsonic polish item, unchanged since first
   recorded. The stream and the file are correct; only the attributes are
   unpopulated.
5. **Cancel semantics leave a partly-acquired release partly acquired** until
   the next sync re-enqueues it. By design, but a GA cut should decide
   deliberately whether re-enqueue is automatic or user-visible.

## B. Documentation hygiene for the cut

6. ~~**Stale OPEN markers in `docs/BETA_ACCEPTANCE.md`.**~~ **ADDRESSED in
   this PR:** the three markers (DJI-489, DJI-490, the tag-canonicalisation
   finding) now carry inline `RESOLVED — see …` pointers to their resolving
   sections/rows.
7. ~~**The entrance-coverage table should narrow its "not driven live"
   claim**~~ **ADDRESSED in this PR:** the fallback row now says the entrance
   itself is driven live (DJI-501) and what remains undriven is specifically
   the mismatched-work refusal on it (gap A1).
8. ~~**`CHANGELOG.md`'s [Unreleased] is empty while four PRs landed after
   `v0.0.2-beta.1`**~~ **ADDRESSED in this PR:** #250–#253 are entered under
   `[v0.0.2]` and the tag is cut on this PR's merge commit.

## C. Test / CI gaps

9. **Mutation proofs for browser behavior are manual procedures.** DJI-434's
   gate-removal proof is recorded in Linear; the DJI-501 proof (remove
   `YTDLP_PROXY` → spec fails) likewise. A GA cut could either accept manual
   proofs as policy or add a scheduled mutation job; today neither is written
   down as a decision.
10. **The boundary is only refusal-tested.** No spec drives a *successful*
    proxied download through an allowlisted domain, so a config mistake that
    blocks everything would still pass the suite. A success-path probe needs a
    stable, allowlisted public file host usable from CI egress.

## Suggested cut order

B6–B8 are cheap doc/entry work and unblock a clean `v0.0.2` tag. A3 and A4 are
the smallest behavior items. A1/A2 are the real GA gates; A5/C9 are decisions
to record, not necessarily code.
