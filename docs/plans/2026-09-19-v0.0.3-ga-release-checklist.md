# v0.0.3 GA release checklist — drafted 2026-09-19

> Assessed after PR #259 (`9eebb75`): the v0.0.2 GA gap list
> (`docs/plans/2026-09-19-v0.0.2-ga-gap-list.md`) is fully struck, the
> Beta Readiness Linear project has no open issues, and CHANGELOG's
> `[Unreleased]` is empty. One blocker and four pre-tag steps remain.

## 0. Blocker — must land before the tag

- [ ] **Un-red `e2e.yml` on master.** `egress-refusal.spec.ts` fails in CI
  with `Test timeout of 30000ms exceeded` (270 passed / 1 failed, all
  retries burned; 3 consecutive master runs red as of 2026-09-19 19:48).
  Worker-driven specs need a per-test timeout far above Playwright's 30s
  default: set `test.setTimeout()` per slow test or a `timeout` override in
  `playwright.config.ts` for the worker-exercising specs. Verify a green
  master run end to end — do not tag with the acceptance gate red.

## 1. CI hardening

- [ ] Scheduled mutation job (see design chat): `mutation-check.sh` wired as
  a weekly `workflow_dispatch`+`schedule` workflow, both mutations green.
- [ ] `mutation-check.sh` resolves Python portably (`command -v python3 ||
  command -v python`), so the CI runner can execute the `gate` mutation.
- [ ] The scheduled job's first successful run is referenced from
  `docs/BETA_ACCEPTANCE.md` (mutation proofs are no longer session-only).

## 2. Evidence refresh for the tag

- [ ] Re-run the full Playwright suite on master after the timeout fix;
  record the green run date in `docs/BETA_ACCEPTANCE.md`.
- [ ] `docs/BETA_DEPLOYMENT.md` smoke pass against the release-candidate
  image (fresh clone → documented steps → `scripts/beta-smoke.sh` green),
  same procedure as the v0.0.2 verification.
- [ ] Confirm test-API endpoints remain gated: `E2E_ENABLE_TEST_API` unset
  in the beta overlay must yield 404 for all three `/api/test/*` routes.

## 3. Docs & bookkeeping

- [ ] CHANGELOG: move #259 under `### Added` (fake-slskd seam, ga-probes,
  multi-hop live drive, mutation harness, success-path probe).
- [ ] `docs/plans/2026-09-19-v0.0.2-ga-gap-list.md`: add a "delivered in
  v0.0.3" header pointing at the tag.
- [ ] Linear: confirm Beta Readiness stays empty; file any post-audit
  residuals (main.go test-fixture owner, audio-probe entrypoint split,
  cleanup-endpoint name duplication) in the backlog project.

## 4. Cut

- [ ] Tag `v0.0.3` at the green master commit; release notes from the
  CHANGELOG section.
- [ ] Fresh-clone + documented-steps bring-up of the tagged image;
  `beta-smoke.sh` every check passing.
- [ ] Post-tag: `git status` clean, clone removed, report environment
  hand-off (beta stack state, e2e stack state, volumes touched).
