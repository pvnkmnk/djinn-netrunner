# Destinations yt-dlp may fetch through the validating egress boundary
# (DJI-501). One domain per line, leading dot includes subdomains.
#
# This is the beta default: YouTube and its regional/CDN hosts, which is what
# the fallback entrance extracts from today. Extend this file when a feed
# needs another extraction site -- the proxy refuses everything not listed,
# which is the point of the boundary.
.youtube.com
.ytimg.com
.googlevideo.com

# E2E-only: the wrong-work refusal probe's server (ops/audio-probe, mounted
# by the e2e overlay). It resolves to a documentation-IPv6 address
# (2001:db8:aa::/64, RFC 3849) that is unreachable from the internet, so
# allowlisting it here costs nothing in beta -- but the entry exists only so
# the e2e probe's download passes the BOUNDARY and is refused by the identity
# gate, which is the layer that spec proves. No production feed should ever
# name this host.
audio-probe.e2e.test
