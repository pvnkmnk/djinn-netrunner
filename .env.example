# PostgreSQL
POSTGRES_PASSWORD=changeme_secure_password

# slskd Soulseek credentials
SLSKD_USERNAME=your_soulseek_username
SLSKD_PASSWORD=your_soulseek_password
SLSKD_API_KEY=generate_random_api_key

# Domain for Caddy (use localhost for local dev)
DOMAIN=localhost

# Notifications (optional)
# Enable job completion webhook notifications
NOTIFICATION_ENABLED=false
# Webhook URL to POST to when jobs complete
NOTIFICATION_WEBHOOK_URL=
