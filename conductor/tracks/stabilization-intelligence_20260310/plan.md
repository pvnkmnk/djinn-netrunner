# Implementation Plan: Stabilization & Intelligence

**Track ID:** stabilization-intelligence_20260310
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-10
**Status:** [ ] Not Started

## Overview

This track focuses on the "Intelligence" layer of the acquisition pipeline. We will implement robust retry strategies, deep-link with Gonic to prevent redundant work, and finalize the database architecture for distributed environments.

## Phase 1: Pipeline Resilience (Backoff & Gonic Integration)

Enhance the worker logic to be "smarter" about when and how it attempts acquisitions.

### Tasks

- [x] Task 1.1: **Exponential Backoff** (0d5feaa): Update `database.JobItem` schema to include `retry_count` and `next_attempt_at`. Implement backoff calculation logic.
- [ ] Task 1.2: **Gonic "Online" Check**: Implement `search3` and `getSong` methods in `GonicClient`.
- [ ] Task 1.3: **Smart Skip**: Update `AcquisitionHandler` to perform a pre-flight check against Gonic; if the track is found and "Online", mark the item as `completed (already indexed)`.

### Verification

- [ ] Unit test for backoff duration calculation (1m, 5m, 1h, etc.).
- [ ] Verification that a manually added file in Gonic causes the worker to skip that track.

## Phase 2: High-Fidelity Metadata (Cover Art)

Ensure acquired music meets modern library standards by embedding visual metadata.

### Tasks

- [ ] Task 2.1: **Tag Writer Integration**: Evaluate and integrate a Go library for writing tags (e.g., `bogem/id3v2` or `go-flac`).
- [ ] Task 2.2: **Embedding Logic**: Implement `EmbedCoverArt(filePath string, artData []byte)` in `MetadataExtractor`.
- [ ] Task 2.3: **Import Pipeline Update**: Update `AcquisitionHandler.importFile` to fetch cover art (if available) and embed it before moving to the final library location.

### Verification

- [ ] Manually inspect an imported file using `ffprobe` or a media player to confirm cover art presence.

## Phase 3: Production Data Layer (Research & Design)

Heavily research and prototype the multi-node coordination strategy.

### Tasks

- [ ] Task 3.1: **Coordination Prototyping**: Run side-by-side tests of LiteFS (WAL shipping) vs. rqlite (Statement replication).
- [ ] Task 3.2: **Locking Assessment**: Evaluate if current file-based locking is sufficient for multi-node NFS/EFS mounts or if a dedicated DLM (Distributed Lock Manager) is needed.
- [ ] Task 3.3: **Final Schema Design**: Finalize the "Production Version" of the database schema, ensuring all migration paths are clear.

### Verification

- [ ] Technical Design Document (ADR) finalized and approved.

## Phase 4: Distributed Coordination Implementation

Execute the migration to the finalized production data layer.

### Tasks

- [ ] Task 4.1: **Migration Execution**: Implement the chosen distributed pattern (e.g., LiteFS configuration).
- [ ] Task 4.2: **Multi-node Verification**: Spin up 2+ worker nodes and verify they do not double-claim jobs or corrupt the SQLite file.

### Final Verification

- [ ] All acceptance criteria met
- [ ] Multi-node acquisition confirmed safe
- [ ] Ready for final review

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
